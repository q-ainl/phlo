{
    "name": "q-ainl/phlo",
    "description": "Phlo, a compile-to-PHP web framework with its own .phlo language, built-in SPA runtime, dashboard and agent-first introspection.",
    "type": "library",
    "license": "MIT",
    "keywords": ["framework", "compiler", "spa", "frankenphp", "agent-first", "phlo"],
    "homepage": "https://github.com/q-ainl/phlo",
    "authors": [
        {
            "name": "Jordi Boerman",
            "email": "info@q-ai.nl"
        }
    ],
    "require": {
        "php": ">=8.3"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0 || ^12.0"
    },
    "suggest": {
        "ext-pdo": "Required by the DB resources (MySQL, PostgreSQL, SQLite)",
        "ext-sodium": "Required by the security/encryption resource",
        "ext-gd": "Required by the icon sprite builder (build config key: icons)"
    },
    "autoload": {
        "files": ["phlo.php"]
    },
    "scripts": {
        "test": "phpunit"
    },
    "config": {
        "sort-packages": true
    }
}
