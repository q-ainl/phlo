@ summary: DB-agnostic ORM test fixture. Sets the default engine via the same
@          injection idiom apps use, pointing it at a dependency-free SQLite file.

static %model.DB => %SQLite(data.'test.db')

static %jdoc.DB => %JSONDB(data.'jdocs.json')

app::route()

route GET => view('ok', 'DB')
